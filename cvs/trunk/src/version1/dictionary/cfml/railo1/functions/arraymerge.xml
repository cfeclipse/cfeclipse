<?xml version="1.0" encoding="UTF-8"?>

<!--
	Array ArrayMerge(array a1, array a2[, boolean leaveIndex])
-->
<function xmlns="http://www.cfeclipse.org/version1/dictionary"
	name="arraymerge" returns="Array">
	<help><![CDATA[
		This function merges two arrays to a single one.
		The third parameter (leaveIndex) defines whether 
		the merged arrays will maintain their index values 
		or if the second array will simply append to the first one.
		If both arrays contain the same index and you specified 
		the parameter leaveIndex to true, the corresponding 
		index in the resulting array will always contain the 
		value of the index of the first array.
		Example:
		<cfdump var="#arraymerge(array(1,2,3),array('a','b','c','d'),true)#">
		Output: {1,2,3,d}
		<cfdump var="#arraymerge(array(1,2,3),array('a','b','c','d'),false)#">
		Output: {1,2,3,a,b,c,d}
	]]></help>
	<parameter name="a1" type="Array" required="true">
		<help><![CDATA[
			The first array to merge.
		]]></help>
	</parameter>
	<parameter name="a2" type="Array" required="true">
		<help><![CDATA[
			The second array to merge.
		]]></help>
	</parameter>
	<parameter name="leaveIndex" type="Boolean" required="true">
		<help><![CDATA[
			If true, only indexes not in a1 will be applied from a2.
			If false, the indexes of a2 will be increased by the length of a1.
		]]></help>
	</parameter>
</function>
